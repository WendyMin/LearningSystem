import React from 'react';
import { Button } from 'react-bootstrap';
import style from 'style';

type Props = {
  className?: string,
  text: string,
  onClick: () => any
};

class MyButton extends React.PureComponent<Props> {
  constructor( props ){
    super( props );
    //this.state = {
    //  choosed: false
  //  }
  }

  //chose = () => {
    //this.setState ({
      //choosed: !this.state.choosed
    //}//)
//  }



  render(){
      const { className , text , onClick } = this.props;
      //const { choosed } = this.state;
      return (
        //choosed ?
        <Button
          bsStyle="primary"
          className={style.choosed + ' ' + className}
          //onMouseMove={() => this.chose()}
          //onClick={() => {this.chose() ; onClick}}
          onClick={onClick}
        >
          {text}
        </Button>
        //:
        //<div
          //bsStyle="primary"
          //className={style.basic + ' ' + className}
          //onMouseMove={() => this.chose()}
        //  onClick={onClick}
      //>
          //{text}
      //  </div>
      );
  }
};

export default MyButton;
