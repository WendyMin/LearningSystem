
import React from 'react';
import { bindActionCreators } from 'redux';
import { connect } from 'react-redux';
import Button from 'UI/Button';
import * as actionCreators from 'actions';

import style from 'style';

class WriteContent extends React.PureComponent {

  render(){
    const {
      mainContent,
      submiter,
      loader,
      requestData
    } = this.props;



    return (
      <div className="container">
        <p className={style.title}> &nbsp;&nbsp;&nbsp;{mainContent.title} </p><br/>
        <p className={style.content}> &nbsp;&nbsp;&nbsp;&nbsp;{mainContent.content} </p>
      </div>
    );
  }
};

export default connect(
  ({ WriteContent: ownState }) => ({
    mainContent: ownState.content,
    loadingData: ownState.loadingData,
  }),
  dispatch => bindActionCreators( actionCreators , dispatch )
)( WriteContent );
